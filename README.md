# CipherLink

End-to-end encrypted messenger with React frontend and Node.js backend.

## Structure
- `backend/` API + WebSocket + DB config
- `frontend/` UI + crypto + ws client
- `docker-compose.yml` local stack

## Local run
1. `cd backend && npm install && npm run dev`
2. `cd frontend && npm install && npm run dev`
