apiVersion: v1
kind: Service
metadata:
  name: cipherlink-frontend
  namespace: cipherlink
spec:
  selector:
    app: cipherlink-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP
