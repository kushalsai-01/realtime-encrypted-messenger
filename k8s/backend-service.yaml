apiVersion: v1
kind: Service
metadata:
  name: cipherlink-backend
  namespace: cipherlink
spec:
  selector:
    app: cipherlink-backend
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
  type: ClusterIP
